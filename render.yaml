services:
  - type: web
    name: groww-review-insight-backend
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/render_backend.py
    healthCheckPath: /health
